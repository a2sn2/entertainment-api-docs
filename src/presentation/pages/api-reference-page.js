import { codeBlock } from '../components/code-block.js';
import { fieldTable, simpleTable } from '../components/data-table.js';
import { escapeHtml } from '../components/dom-utils.js';
const list=(items)=>`<ul>${items.map((item)=>`<li>${escapeHtml(item)}</li>`).join('')}</ul>`;
export function renderApiReferencePage(root, repository) {
  const apis=repository.getApis();
  return `<header class="page-header page-enter"><div class="page-kicker">Detailed technical contract</div><h1 class="page-title">API Reference</h1><p class="page-lead">Five documented operations with explicit headers, fields, examples, success criteria, business rules, and retry guidance.</p></header><section class="section"><div class="callout"><div>ℹ️</div><div><h3>Shared operations endpoint</h3><p>Catalog and purchase operations use <code>/api/v1/acs/ops/prs_un_enc</code>. The <code>theType</code> property selects the backend operation.</p></div></div></section><section class="section">${apis.map((api)=>`<article class="api-panel page-enter" id="${api.ref.toLowerCase()}"><div class="api-panel-head"><div><div class="api-ref">${api.ref}</div><h2>${escapeHtml(api.name)}</h2><p>${escapeHtml(api.purpose)}</p></div><div><span class="badge badge-method">${api.method}</span> <span class="badge badge-success">${api.status}</span></div></div><div class="api-meta"><div><span>Path</span><strong class="mono">${escapeHtml(api.path)}</strong></div><div><span>Authentication</span><strong>${escapeHtml(api.auth)}</strong></div><div><span>Content Type</span><strong>application/json</strong></div><div><span>Domain</span><strong>${escapeHtml(api.group)}</strong></div></div><div class="api-panel-body"><div class="tabs" data-tabs><div class="tab-list" role="tablist"><button class="tab-btn" data-tab="request" aria-selected="true">Request</button><button class="tab-btn" data-tab="response" aria-selected="false">Response</button><button class="tab-btn" data-tab="rules" aria-selected="false">Rules</button><button class="tab-btn" data-tab="errors" aria-selected="false">Errors</button></div><div class="tab-panel" data-panel="request"><h3>Request headers</h3>${simpleTable(['Header','Required','Value','Description'],api.headers.map((h)=>[`<code>${escapeHtml(h.field)}</code>`,h.required,`<code>${escapeHtml(h.example)}</code>`,escapeHtml(h.description)]))}<h3>Request fields</h3>${fieldTable(api.requestFields,true)}<div style="margin-top:14px">${codeBlock(api.requestExample,'Request JSON')}</div></div><div class="tab-panel" data-panel="response" hidden><h3>Response fields</h3>${fieldTable(api.responseFields,false)}<div style="margin-top:14px">${codeBlock(api.responseExample,'Response JSON')}</div></div><div class="tab-panel" data-panel="rules" hidden><h3>Success criteria</h3>${list(api.success)}<h3>Business rules</h3>${list(api.rules)}</div><div class="tab-panel" data-panel="errors" hidden><h3>Error and retry guidance</h3>${list(api.errors)}</div></div></div></article>`).join('')}</section>`;
}
