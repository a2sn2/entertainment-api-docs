import { badgeClass, escapeHtml } from '../components/dom-utils.js';
export function renderKnownLimitationsPage(root,repository){return `<header class="page-header page-enter"><div class="page-kicker">Evidence labels</div><h1 class="page-title">Known Limitations</h1><p class="page-lead">Every limitation is classified as confirmed, observed, inferred, or pending backend confirmation.</p></header><section class="stack page-enter">${repository.getLimitations().map((item)=>`<div class="fact-row"><span class="badge ${badgeClass(item.label)}">${escapeHtml(item.label)}</span><div>${escapeHtml(item.text)}</div></div>`).join('')}</section>`;}
