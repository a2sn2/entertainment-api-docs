name: FoundationKit CodeQL

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: foundationkit-codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: CodeQL ${{ matrix.language }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: csharp
            build_mode: manual
          - language: javascript-typescript
            build_mode: none

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@7d9249f5a57288b8a472ddd9cc3ece1921f7dc38 # immutable current supported CodeQL Action
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build_mode }}

      - name: Set up .NET 8
        if: matrix.language == 'csharp'
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: "8.0.x"

      - name: Restore C# solution
        if: matrix.language == 'csharp'
        run: dotnet restore FoundationKit.sln

      - name: Build C# solution for CodeQL
        if: matrix.language == 'csharp'
        run: dotnet build FoundationKit.sln --configuration Release --no-restore

      - name: Analyze
        uses: github/codeql-action/analyze@7d9249f5a57288b8a472ddd9cc3ece1921f7dc38 # immutable current supported CodeQL Action
        with:
          category: "/language:${{ matrix.language }}"
